% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{json_state}
\alias{json_state}
\alias{json_revive}
\title{Persist a class the default rule does not fit}
\usage{
json_state(x)

json_revive(class, state)
}
\arguments{
\item{x}{Object whose state is to be recorded.}

\item{class}{Empty object carrying the recorded class vector, which
\code{json_revive()} dispatches on.}

\item{state}{Whatever the matching \code{json_state()} method returned.}
}
\value{
The \code{json_state()} function returns a list, and \code{json_revive()}
the rebuilt object.
}
\description{
Most classes need nothing here: an S3, S4 or S7 object is a base type
plus attributes, so \code{\link[=json_write]{json_write()}} records it without help. A class
whose instances hold something outside that model — a connection opened
by \code{initialize}, a handle to a running process, a reference that has to
be recorded as a key rather than a value — supplies a method for this
pair instead, modelled on the \verb{__getstate__} and \verb{__setstate__} protocol
of Python's pickle.
}
\details{
A \code{json_state()} method returns a plain list of what to persist, and is
free to leave out anything that can be recomputed. The document records
that list next to the class vector of the object. On the way back,
\code{json_revive()} dispatches on the recorded class through an empty object
carrying it, so a method signature always starts with the class token
rather than the object being rebuilt.

Methods for the class generators of both \code{R6} and S7 ship with the
package and follow the same protocol. An \code{R6} instance has no method,
and writing one is refused rather than guessed at; see \code{\link[=r6_state]{r6_state()}} for
why, and for the pair a class author opts in with.
}
\examples{
handle <- structure(list(path = "/tmp/log", con = "a live connection"),
                    class = "file_handle")

json_state.file_handle <- function(x) list(path = x$path)

json_revive.file_handle <- function(class, state) {
  structure(list(path = state$path, con = NULL), class = "file_handle")
}

json_write_str(handle)

json_read_str(json_write_str(handle))

}
